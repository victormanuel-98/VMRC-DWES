# Se debe:

## Ejercicios

### Ejercicio 1
- Crear un proyecto Node.js y ejecutar todos los apartados de la sección **“Cómo empezar”** de la web oficial de ExpressJS.  
- Configurar el servidor básico y probar rutas simples.

---

### Ejercicio 2
- Replicar los ejercicios anteriores hechos con el módulo HTTP nativo **usando ExpressJS**:

1. Servidor que devuelva **Hello World!** en el puerto 4000.  
2. Servidor que devuelva una **página web** en el puerto 3000.  
3. Servidor con distintas páginas según la **URL accedida**.  
4. Crear dos rutas:
   - `/page` → Devuelve una página web.  
   - `/error` → Devuelve una página de error con **código 404**.  
5. Servir las páginas cargadas desde **ficheros .html** en lugar de escribir el HTML en el código.  
6. Enviar un parámetro `name` por **query string** y devolver:
   ```text
   Hello ${name}!
7. Ruta fizzbuzz con query string de un numero, y devuelve toda la secuencia de números hasta
   dicho numero.

### Entregables:

- ejercicio 1
- ejercicio 2
