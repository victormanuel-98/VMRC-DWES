# Se debe:

## Crear una imagen de Docker para una API Node.js sencilla, gestionando dependencias y puerto mediante variables de entorno. Automatizar su despliegue con Docker Compose y asegurar que la imagen ignore archivos innecesarios mediante .dockerignore. Finalmente, comprobar que la API funciona correctamente desde el contenedor.
